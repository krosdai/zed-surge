{
  "grammars": [
    {
      "name": "surge",
      "camelcase": "Surge",
      "scope": "source.surge",
      "// file-types": "Consumed by the tree-sitter CLI only, so `conf` here is safe — Zed takes its suffixes from languages/surge/config.toml instead.",
      "file-types": ["conf", "sgmodule"],
      "injection-regex": "^surge$"
    }
  ],
  "metadata": {
    "version": "0.1.0",
    "license": "MIT",
    "description": "Tree-sitter grammar for Surge configuration files",
    "authors": [{ "name": "xdanger", "email": "y@dai.co" }]
  }
}
